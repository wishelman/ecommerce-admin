PORT=4000
NODE_ENV=development

# MySQL connection (Railway/MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=ecommerce_admin

# JWT
JWT_SECRET=supersecretkey_change_me
JWT_EXPIRES_IN=1d

# Client origin (Vercel URL) for CORS
CLIENT_ORIGIN=http://localhost:5173
